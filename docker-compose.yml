version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./vector_dbs:/app/vector_dbs
      - ./model_cache:/app/model_cache
    env_file:
      - .env
    restart: always
